class Lvl1 extends Lvl {
  constructor() {
    super()
    this.image = lvl1Image;

    this.questionString = `You're not Cheddar, you're ...`

    this.annyangCommand = 'just a common b****'

    this.answerAString = `this is answer A`;
    this.answerBString = `This is answer B`;
    this.answerCString = `This is answer C`;
    this.answerDString = `This is answer D`;
  }

  update() {
    super.update();
  }

  mousePressed() {
    super.mousePressed();
  }
}